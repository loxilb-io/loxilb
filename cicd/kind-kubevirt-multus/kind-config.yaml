# 3-node kind cluster for the KubeVirt + loxilb scenario. The cluster name comes from config.sh (--name).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
